[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "avc"
version = "0.1.0"
description = "Plasma Physics utilities"
authors = [{name = "Jacob Gunn"}]

[tool.setuptools.packages.find]
where = ["."]
